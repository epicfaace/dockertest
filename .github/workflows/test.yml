name: Test
on: push

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
        matrix:
            os: [ubuntu-latest]
            test: [a b,c d]
            x: [true, false]
            exclude:
              - test: c d
                x: true
        fail-fast: false
    steps:
      - uses: actions/checkout@v2
      - run: echo "GOT RUN"
        if: ${{ !matrix.test }}
